import 'package:flutter/material.dart';

/// Judul app bar: "SBPS".
class HomeTitle extends StatelessWidget {
  const HomeTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('SBPS');
  }
}
